import { MathInputFormat } from "../../utils/kotlin-lib-functions";

export type TaskCreationType = "auto" | "manual";

export interface TaskConstructorInputs {
  taskCreationType: "auto" | "manual";
  nameEn: string;
  nameRu: string;
  code: string;
  namespace: string;
  subjectTypes: string;
  startExpression: {
    format: MathInputFormat;
    expression: string;
  };
  goalType: string;
  goalExpression: {
    format: MathInputFormat;
    expression: string;
  };
  goalNumberProperty: number;
  goalPattern: string;
  rulePacks: string;
  stepsNumber: number;
  time: number;
  difficulty: number;
  solution: {
    format: MathInputFormat;
    expression: string;
  };
  countOfAutoGeneratedTasks: number;
  operations: string;
  stepsCountInterval: number;
  implicitTransformationsCount: number;
  autoGeneratedRulePacks: string;
  lightWeightOperations: string;
  nullWeightOperations: string;
  startTime: string;
  endTime: string;
}

export interface TaskConstructorProps {
  index: number;
  defaultValue: any;
  updateDemo: (index: number) => void;
  visualizationMode: "table" | "list";
  hidden?: boolean;
  updateName?: (index: number, newName: string) => void;
}
